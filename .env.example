APP_NAME=GOSTUDY
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=es
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=es_PE

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Base de datos MySQL local (XAMPP)
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=gostudy
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database

MAIL_MAILER=log
MAIL_FROM_ADDRESS="no-reply@gostudy.local"
MAIL_FROM_NAME="${APP_NAME}"

# CORS — frontend Next.js local
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# Sanctum — Bearer ONLY (NO statefulApi)
SANCTUM_TOKEN_PREFIX=

# ─────────────────────────────────────────────────────────────
# 📦 PRODUCCIÓN (Hostinger) — descomenta y ajusta al desplegar
# ─────────────────────────────────────────────────────────────
# APP_ENV=production
# APP_DEBUG=false
# APP_URL=https://apigostudy.dbltech.tech
#
# DB_HOST=localhost
# DB_DATABASE=u123456_gostudy        # nombre real desde hPanel → Bases de datos
# DB_USERNAME=u123456_gostudy
# DB_PASSWORD=clave-real-del-panel
#
# CORS_ALLOWED_ORIGINS=https://gostudy.dbltech.tech
#
# MAIL_MAILER=smtp                   # opcional, si conectas email real
# MAIL_HOST=smtp.hostinger.com
# MAIL_PORT=587
# MAIL_USERNAME=no-reply@dbltech.tech
# MAIL_PASSWORD=
# MAIL_ENCRYPTION=tls

# APIsPERU - consulta DNI/RUC
APISPERU_BASE_URL=https://dniruc.apisperu.com/api/v1
APISPERU_TOKEN=
